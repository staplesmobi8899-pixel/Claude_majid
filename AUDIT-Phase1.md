# تقرير Phase 1 — Core Foundation Audit

> هذا تقرير **قراءة وتوثيق** فقط. لم يُعدّل أي كود في `Athar-Platform.html`. لا تنفيذ قبل اتفاق المالك (محمد).

**الملف:** `Athar-Platform.html` — 4946 سطرًا (CSS: 10–2256 · HTML: 2258–2876 · JS: 2878–4944)
**النسخة:** مطابقة لـ `Athar-Platform-v4-stable.html` (تمت استعادتها من الـ artifact، 183,269 بايت، مطابقة لطول RSC المعلن).

---

## 0) فحص السلامة الآلي (PASS ✅)

| الفحص | النتيجة |
|---|---|
| `node --check` على الـ JS المستخرج (2067 سطرًا) | ✅ **JS SYNTAX OK** |
| دوال مفقودة (كل `onclick`/`oninput` له تعريف) | ✅ **0 مفقودة** — كل الـ 35 دالة handler معرّفة مرة واحدة بالضبط |
| تعريفات مكررة لنفس الدالة | ✅ **0 تكرار** |
| `DOCTYPE` / `</html>` | ✅ فرديان |
| الصفحات السبع (page divs) | ✅ موجودة كلها |
| الـ Services (User/Campaign/Partner) + CONFIG/T/Validators/DB/Models/State/AdminUI | ✅ موجودة كلها |

**تسلسل التهيئة (`DOMContentLoaded`, سطر 2042):** `initData()` → `applyI18n()` → استعادة الجلسة من `DB.getObj(SESSION)` → `State.setUser()` → `enterApp()` → ربط مغلقات الـ modal. نظيف ومغلّف بـ `try/catch`. ✅

**الخلاصة:** البنية المنطقية (JS) سليمة. المشاكل كلها في **طبقة التصميم (CSS tokens / التوحيد)** — وهي بالضبط هدف Phase 1.

---

## 1) Theme Tokens — القيم المكتوبة يدويًا (المشكلة الأكبر)

### 1.أ — ألوان hex تكرّر متغيّرات موجودة أصلًا (يجب استبدالها بـ `var()`)

14 لونًا مكتوبًا يدويًا يطابق متغيّرًا موجودًا في `:root`:

| المكتوب يدويًا | المتغيّر المقابل |
|---|---|
| `#5a6b3a` | `--olive` |
| `#6b7a4f` | `--olive-mid` |
| `#8a9a6a` | `--olive-light` |
| `#eef1e8` | `--olive-pale` |
| `#f5ede1` | `--sand` |
| `#e8d9c4` | `--sand-dark` |
| `#fdfaf6` | `--warm-white` |
| `#1a1a1a` | `--charcoal` |
| `#888888` | `--charcoal-50` |
| `#d4d4d4` | `--charcoal-20` |
| `#ffffff` | `--white` |
| `#c84c3c` | `--red` |
| `#2d7a4f` | `--success` |
| `#c47d1a` | `--warn` |

### 1.ب — ألوان بلا توكنات (عائلات ناقصة يجب إضافتها للنظام)

- **أسطح داكنة:** `#121810` (خلفية login/hero، ×3) · `#1a2010` → مقترح: `--surface-dark`, `--surface-dark-2`.
- **ألوان شارات الحالة (badges):** `#166534`/`#f0fdf4` (نجاح) · `#991b1b`/`#fef2f2` (خطأ) · `#92400e`/`#fef9ec` (تحذير) · `#3730a3` · `#f7f4ee` → مقترح مجموعة `--badge-*-fg/-bg`.

### 1.ج — قيم `rgba()` مكتوبة يدويًا

**100 قيمة rgba** في الـ CSS، كثير منها تكرار لألوان التوكنات مع شفافية:
- `rgba(232,217,196,0.x)` = `--sand-dark` بشفافية → مستخدمة **25+ مرة** بقيم alpha مختلفة (0.4/0.5/0.6/0.7…).
- `rgba(90,107,58,0.x)` = `--olive` بشفافية → 12+ مرة.
- `rgba(255,255,255,0.x)` و`rgba(0,0,0,0.x)` → عشرات.

**المقترح:** تعريف توكنات alpha (مثل `--olive-a40`, `--sand-line`) أو استخدام `color-mix(in srgb, var(--token) X%, transparent)`.

---

## 2) Spacing System — **غير موجود** (يجب إنشاؤه)

- **210** تصريح `padding/margin/gap` مكتوب يدويًا.
- `gap` وحده يستخدم **14 قيمة مختلفة**: `0.22 · 0.28 · 0.4 · 0.45 · 0.5 · 0.6 · 0.7 · 0.75 · 0.8 · 0.85 · 0.9 · 1 · 1.1 · 1.2rem` — لا مقياس موحّد.

**المقترح — مقياس `--sp-*`** (نقرّبه ونوحّد القيم القريبة):

```
--sp-1: 0.25rem   --sp-2: 0.5rem   --sp-3: 0.75rem   --sp-4: 1rem
--sp-5: 1.25rem   --sp-6: 1.5rem   --sp-7: 2rem      --sp-8: 2.5rem   --sp-9: 3rem
```

---

## 3) Typography System — **غير موجود** (يجب إنشاؤه)

- **~38 قيمة font-size مختلفة** بين `0.62rem` و`3rem`، بتزاحم واضح وقيم شبه مكررة: `0.82/0.83/0.84/0.85/0.86/0.88rem` كلها متجاورة بلا سبب.
- الأوزان مبعثرة (400–900) بلا مقياس مسمّى.

**المقترح — مقياس `--fs-*`** (دمج القيم القريبة في درجات):

```
--fs-xs: 0.7rem    --fs-sm: 0.8rem    --fs-base: 0.9rem   --fs-md: 1rem
--fs-lg: 1.25rem   --fs-xl: 1.5rem    --fs-2xl: 2rem      --fs-3xl: clamp(2.2rem,7vw,3.6rem)
```

(الـ `clamp()` في عناوين hero يُبقى — جيد للاستجابة.)

---

## 4) Shared Layouts

- لا يوجد container/wrapper مشترك للصفحات؛ كل صفحة تعرّف padding أفقيًا خاصًا بها (نمط `padding: … 1.5rem` متكرر). الكلاس المشترك الوحيد القريب هو `.section` (سطر 487).
- **المقترح:** كلاس حاوية موحّد (مثل `.page-inner` بـ `max-width` + padding أفقي عبر `--sp-*`) لترث منه كل الصفحات بدل تكرار القيم.

---

## 5) Responsive & Safe Areas

- **نقاط الكسر غير متسقة:** خليط من `max-width` و`min-width`: `380px (×2)` · `640px` · `768px (min)` · `1024px (min)`. لا مجموعة breakpoints موحّدة.
- **safe-area-inset:** مستخدمة **4 مرات فقط** (bottom nav + بعض الـ modals). **يجب التحقق** أن الـ navbar العلوي (`position:fixed; top:0; height:60px`) يحترم `env(safe-area-inset-top)` على iPhone — يبدو أنه لا يفعل (نقطة اختبار على جهاز حقيقي).
- inputs على iOS: يجب التأكد أن كل input ≥ `16px` لمنع auto-zoom (للفحص في Phase 2).

---

## 6) RTL / LTR

- `applyI18n()` تبدّل `documentElement.dir` (4 إشارات). ✅
- **16** استخدامًا لخصائص فيزيائية `left/right` مقابل **17** استخدامًا لخصائص منطقية. بعض الـ `left/right` مشروع (navbar الثابت، تدرّجات زخرفية)، لكن بعضها يجب تحويله لخصائص منطقية (`inset-inline-*`). يحتاج مراجعة بندًا-بندًا في Phase 3.

---

## 7) CSS مكرر / يتيم

- **27 كلاس** معرّف في الـ CSS وغير مُشار إليه في HTML/JS (مرشّحون للحذف بعد تأكيد):
  معظمها بقايا أدمن: `adm-bar-fill/track/top` · `adm-detail-k/v/row` · `adm-lead-name/rank/val` · `adm-notif-*` · `adm-quick-ico` · `adm-modal-title` · بالإضافة إلى `app-wrap` · `tab-bar` · `tab-btn` · `count-pill` · `empty-sub` · `pill-btn-olive` · `admin-page*`.
  > ملاحظة: قد يكون بعضها false positive (يُطبّق ديناميكيًا) — يُؤكَّد قبل الحذف.

---

## الخلاصة والتوصية لـ Phase 1

**لا حاجة لإعادة كتابة.** البنية المنطقية سليمة. العمل المطلوب = **توحيد طبقة التصميم** ضمن `:root` + استبدال القيم اليدوية بـ توكنات، كوحدة CSS واحدة منسّقة، مع إبقاء الـ JS كما هو.

### نطاق التنفيذ المقترح (بعد اتفاقك)
1. إضافة توكنات `--sp-*` و`--fs-*` و(ألوان السطح الداكن + الشارات + alpha) إلى `:root`.
2. استبدال الـ 14 لون hex المكرّر + قيم rgba المكرّرة بـ `var()`.
3. استبدال قيم spacing/font-size اليدوية بالمقياس الموحّد (دفعات مُتحقَّق منها).
4. توحيد breakpoints + إضافة `safe-area-inset-top` للـ navbar.
5. تنظيف الكلاسات اليتيمة المؤكَّدة.
6. بعد كل دفعة: `node --check` + فحص الدوال + فتح الصفحة فعليًا (login → تنقّل → فتح لوحة الأدمن).

**لن ألمس الكود قبل موافقتك على هذا النطاق.**
