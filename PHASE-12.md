# Phase 12 — Integrity, Privacy & Community 2.0

> بريف تنفيذي لـ Claude. القيد الأساسي: **ملف واحد** `Athar-Platform.html` (HTML/CSS/JS صِرف، localStorage فقط، بدون أي backend/إطار/بناء، يعمل offline). التواصل بالعربية. لا إعادة تصميم — حافظ على الهوية والتوكنات الحالية. كل دفعة: `node --check`/vm + jsdom + ربط على branch `claude/new-session-cu79nw` (commit + push).

---

## أ) الهدف
معالجة فجوات في **سلامة البيانات (data integrity)**، **الخصوصية**، **منطق الإشعارات والموافقات**، وترقية **المجتمع** (مجتمعات + هاشتاغات + نشر الصور). الأولوية للإصلاحات الوظيفية قبل الميزات الجديدة.

## ب) القيود التقنية
- لا سيرفر: كل شيء عبر خدمات localStorage الحالية (`CampaignService`, `StatsService`, `NotificationService`, `CommunityService`, `AchievementService`, `CertificateService`, `ActivityService`…).
- إعادة استخدام التوكنات والمكوّنات والأيقونات (SVG thin-line) ونظام الـ elevation الموجود. بدون إيموجي.
- كل النصوص الجديدة عبر `t()` بنسختَي `ar` + `en`.

---

## ج) بنود العمل

### 1) إصلاح: مغادرة الحملة لا تُنقص الساعات وعدد الحملات  ⚠️ Bug
- **الحالي:** عند الانضمام تزيد الساعات/عدد الحملات، لكن عند **المغادرة** لا تُخصم.
- **المطلوب:** `leaveCampaign(campId)` يعكس تمامًا أثر الانضمام: خصم ساعات تلك الحملة من إجمالي ساعات المستخدم، وإنقاص عدّاد «الحملات» بمقدار 1، وإزالة الحملة من `حملاتي`، وتحديث أي إنجازات/leaderboard مشتقّة.
- **معايير القبول:** انضمام ثم مغادرة يُرجِع كل العدّادات (ساعات/حملات/الأثر) إلى قيمتها السابقة بدقّة. لا تنزل القيم تحت الصفر. تُحدَّث واجهة البروفايل فورًا.
- **تقني:** تأكّد أن مصدر حساب الساعات واحد (احسبها من قائمة الحملات المنضَمّ إليها فعليًا بدل عدّاد تراكمي منفصل، لتفادي عدم التزامن).

### 2) خصوصية: إخفاء أسماء المشاركين في الحملة  ⚠️ Privacy
- **المطلوب:** في قائمة المشاركين الظاهرة للمستخدمين العاديين، يظهر **أول حرف أو حرفين فقط** والباقي نجوم (مثال: «محمد» → «م•••» / «مح•••»). الاسم الكامل يبقى ظاهرًا فقط لصاحبه وللأدمن.
- **معايير القبول:** دالة `maskName(name)` موحّدة تُستخدم في كل أماكن عرض أسماء المشاركين. تعمل مع العربية والإنجليزية وتحترم RTL. الأدمن والمستخدم نفسه يشوفون الاسم كاملًا.
- **تقني:** ميّز السياق (admin / self / other) عبر `Permissions`/الجلسة الحالية.

### 3) إصلاح منطق إشعارات منشئ الحملة  ⚠️ Logic
- **الحالي:** منشئ الحملة يستقبل إشعارات «انضمّ المستخدم…» قبل الموافقة.
- **المطلوب:** قبل موافقة الأدمن، المنشئ **لا** يرى إشعارات انضمام. الذي يصله هو فقط **«تمت الموافقة على طلبك»** عند اعتماد الأدمن للحملة. بعد الاعتماد والنشر، تُستأنف إشعارات الانضمام بشكل طبيعي.
- **معايير القبول:** حملة بحالة `pending` لا تولّد إشعارات join للمنشئ. عند الانتقال إلى `approved` يُرسَل إشعار اعتماد واحد للمنشئ.

### 4) شارة «أُنشئت بطلب من متطوّع» على تفاصيل الحملة
- **المطلوب:** الحملة التي أنشأها متطوّع (لا الأدمن) وتمّت الموافقة عليها تظهر للعامة، وعند فتح تفاصيلها يظهر **بأسفل الصفحة** سطر: «أُنشئت هذه الحملة بطلب من متطوّع».
- **معايير القبول:** يظهر السطر فقط للحملات ذات `origin = volunteer` (أو ما يعادلها)، ولا يظهر لحملات الأدمن. ثنائي اللغة.
- **تقني:** أضِف للحملة حقل `origin` (`admin` | `volunteer`) أو استخدم `createdByRole` الموجود.

### 5) «أبرز المتطوّعين» في الرئيسية — Top 10 مع عرض 3 وتوسعة
- **المطلوب:** احسب أعلى **10** متطوّعين بعدد ساعات المشاركة، اعرض **أول 3 فقط**، وعند الضغط يتوسّع القسم ليُظهر العشرة كاملين مع معلومات إضافية.
- **معايير القبول:** الترتيب تنازلي بالساعات (كسر التعادل بالاسم/تاريخ الانضمام). زرّ/إيماءة «عرض الكل» تفتح العشرة، وتنغلق مجددًا. تُحترم خصوصية الاسم (بند 2) حسب السياق.
- **تقني:** اشتقّ من `StatsService`/leaderboard الموجود. لا تكسر تصميم البطاقات الحالي.

### 6) إصلاح: نشر الصور في المجتمع  ⚠️ Bug
- **الحالي:** عند نشر منشور بصورة، يُنشَر النص فقط وتضيع الصورة.
- **المطلوب:** تُحفظ الصورة (base64) ضمن `media[]` للمنشور وتظهر في الموجز ضمن `media-grid`.
- **معايير القبول:** إنشاء منشور بصورة → تظهر الصورة فورًا في الموجز وبعد إعادة التحميل (مخزّنة في DB). دعم منشور نص+صورة معًا. ضغط/حدّ حجم معقول للصورة قبل التخزين.

### 7) مجتمعات (Communities) بدل «المجموعات»
- **المطلوب:** احذف خيار «المجموعات» (Groups scaffold)، وأضِف **«إنشاء مجتمع»** في صفحة المجتمع: أي مستخدم يُنشئ مجتمعًا (اسم/وصف/صورة)، والمستخدمون يقدرون **الانضمام** إليه مثل غروبات فيسبوك/إنستغرام.
- **معايير القبول:**
  - `CommunityGroupService` (DB: `athar_communities`): نموذج `{id, name, desc, cover, owner, members[], createdAt}` + create/join/leave/getAll/getFor.
  - صفحة المجتمع: تبويب/قسم «المجتمعات» يعرض المجتمعات، زرّ إنشاء، وحالة «انضممت/انضمام».
  - منشورات يمكن ربطها بمجتمع (اختياري ضمن هذه الدفعة أو التالية).
- **تقني:** أعِد استخدام مكوّنات البطاقات والنماذج الحالية. RTL/responsive.

### 8) هاشتاغات (#) + بحث
- **المطلوب:** أي مستخدم يكتب هاشتاغ داخل منشور؛ تُستخرج الهاشتاغات تلقائيًا وتصبح **قابلة للنقر**، وبحث يعرض كل المنشورات التي تحوي الهاشتاغ نفسه.
- **معايير القبول:**
  - parser يلتقط `#كلمة` (عربي/إنجليزي/أرقام) ويحوّلها روابط.
  - نقر الهاشتاغ أو البحث `#x` يعرض كل المنشورات الحاوية له (الأحدث أولًا).
  - شريط/أيقونة بحث في المجتمع تدعم البحث بالهاشتاغ والنص.
- **تقني:** فهرسة بسيطة عند الإضافة، أو مسح `getFeed()` وقت البحث. احترس من XSS — نظّف النص قبل إدراج HTML.

### 9) «حملاتي» في البروفايل — مُشارَكة vs مُنشأة
- **المطلوب:** يعرض **كل** الحملات التي شارك فيها المستخدم **و** التي أنشأها. المُشارَك فيها يُوسَم «مشاركة»، والتي أنشأها تُوسَم «أنت أنشأت هذه الحملة».
- **معايير القبول:** وسوم واضحة لكل بطاقة، بدون تكرار حملة ظهرت بالحالتين (إن كان منشئًا ومشاركًا، أظهر الوسمين أو الأولوية «أنشأت»). ثنائي اللغة.

### 10) اعتماد حملات المتطوّعين قبل الظهور + صورة اختيارية
- **المطلوب:** أي حملة يُنشئها مستخدم **لا تظهر** في الرئيسية/الاستكشاف حتى **يوافق الأدمن** ويعدّل عليها. إن أنشئت بدون صورة، تظهر **عند الأدمن** بدون صورة (placeholder) ليكمّلها.
- **معايير القبول:** حملة `pending` غير مرئية للعامة، مرئية في طابور الأدمن. بعد التعديل/الاعتماد تصبح عامة. حملة بلا صورة تظهر للأدمن بـ placeholder واضح وحقل لإضافة صورة.
- **تقني:** اربطه ببند 3 (الإشعارات) وبند 4 (شارة المصدر) — منطق حالة واحد متّسق `pending → approved/rejected`.

---

## د) بنود مُنجَزة سابقًا (تحقّق فقط — لا إعادة تنفيذ)
- **بطاقة «مكافآت ومزايا قادمة»** بأسفل صفحة «من نحن». ✅ (مُنفَّذة)
- **«شهاداتك» + شارة «قيد الانتظار/Pending»** بدل رسالة «أكمل حملة…». ✅ (مُنفَّذة)
- **إلغاء «سجل النشاط»** من البروفايل (الإشعارات تغطّيه). ✅ (مُنفَّذة)

---

## هـ) ترتيب الدفعات (متحقَّق منها + لينك بعد كل دفعة)
| دفعة | المحتوى | مخاطرة |
|---|---|---|
| **12.0** | بند 1 (إصلاح الساعات/العدّاد عند المغادرة) + بند 9 (حملاتي: مشاركة/أنشأت) — منطق بيانات أساسي | متوسطة |
| **12.1** | بند 10 (اعتماد الأدمن قبل الظهور + صورة اختيارية) + بند 3 (إشعارات المنشئ) + بند 4 (شارة المصدر) — منظومة حالة واحدة | متوسطة–عالية |
| **12.2** | بند 2 (إخفاء الأسماء) + بند 5 (أبرز المتطوّعين Top10/3) — خصوصية وعرض | متوسطة |
| **12.3** | بند 6 (إصلاح نشر الصور) + بند 8 (هاشتاغات + بحث) — المجتمع: محتوى | متوسطة |
| **12.4** | بند 7 (مجتمعات بدل المجموعات) — المجتمع: بنية | متوسطة–عالية |

> كل دفعة وحدة مكتملة (HTML+CSS+JS) → `node --check` + فحص الدوال/الـ onclick + jsdom + تحقّق ثنائي اللغة + ربط. بدون كسر السلوك القائم.

## و) معايير التحقق العامة
- JS سليم (vm) · كل `onclick` معرّف · كل مفتاح i18n له `ar`+`en` · jsdom لكل سيناريو قبول · RTL/responsive · بدون إيموجي · بدون regressions في العدّادات/الإشعارات.

## ز) قرارات للاتفاق قبل البدء
1. **حساب الساعات (بند 1):** نعتمد المصدر المشتق (نحسب من الحملات المنضَمّ إليها) — موصى — أم نُبقي العدّاد التراكمي مع إصلاح الخصم فقط؟
2. **ربط المنشورات بالمجتمعات (بند 7):** ضمن 12.4 أم نؤجّله لدفعة لاحقة (12.5)؟
3. **شكل قناع الاسم (بند 2):** حرف واحد + نجوم أم حرفين + نجوم كافتراضي؟
