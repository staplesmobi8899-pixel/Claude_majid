# ATHAR — خريطة السلسلة المترابطة (Admin → كل الحسابات)

تشرح هالوثيقة كل فعل بيعمله الأدمن، وين بيأثّر، وهل بينحفظ بـ Supabase وبيوصل
لباقي الحسابات. الرموز: ✅ شغّال · ⚠️ فجوة لازم تنصلّح.

> ملاحظة أساسية: بعد ضبط دور الأدمن (`role = 'admin'`) صارت كل كتابات الأدمن
> مسموح فيها من RLS. الباقي تفاصيل أدناه.

---

## 1) كيف تنتقل البيانات بين الأجهزة (العمود الفقري للسلسلة)

| الآلية | متى تشتغل | تجلب من Supabase؟ |
|---|---|---|
| `Sync.pullAll()` | عند تسجيل الدخول + استعادة الجلسة (فتح التطبيق) | ✅ نعم |
| `LiveService` (poll كل 12ث) | طول ما التطبيق مفتوح | ⚠️ **لأ** — بس يعيد رسم البيانات المحلية، ما بيجلب جديد |
| على التركيز/visibilitychange | — | ⚠️ **غير موجود** |

**⚠️ الفجوة #1 (الأهم):** ما في **مزامنة حيّة**. لما الأدمن يعمل تغيير، بينحفظ بـ
Supabase، بس جهاز المستخدم التاني ما بيشوفه إلا لما **يعيد فتح التطبيق**. هاد سبب
إنك كنت تضطر "تطلع وترجع".

---

## 2) أفعال الأدمن على المستخدمين

| الفعل | يحفظ بـ Supabase | يُشعِر المستخدم | ملاحظات |
|---|---|---|---|
| قبول مستخدم | ✅ `profiles.status='approved'` | ✅ "تم تفعيل حسابك" | المستخدم يقدر يتفاعل بعد إعادة الفتح |
| رفض مستخدم | ✅ `status='rejected'` | ⚠️ **ما في إشعار** | |
| حظر | ✅ `ban_until/status` | ✅ "تم تعليق حسابك" | |
| رفع الحظر | ✅ | ⚠️ **ما في إشعار** | |
| تعيين دور | ✅ `role` | ⚠️ **ما في إشعار** | |
| تعديل بيانات | ✅ للاسم/الهاتف/المدينة | ⚠️ **تاريخ الميلاد ما بينحفظ** (مفقود من التحويل) | |

## 3) أفعال الأدمن على الحملات

| الفعل | يحفظ بـ Supabase | يُشعِر | ملاحظات |
|---|---|---|---|
| قبول حملة | ✅ `status='approved'` | ✅ المنشئ | تظهر للكل بعد إعادة الفتح |
| رفض حملة | ✅ `status='rejected'` | ✅ المنشئ | |
| قيد التنفيذ | ✅ | — | |
| إنهاء (done) | ✅ + `completed_at` | ✅ المنشئ + المشاركين | ينتج عنه شهادات (لما تفعّلها) |
| تحديد طارئة | ✅ `emergency` | — | تتصدّر الرئيسية بالأحمر |
| إلغاء/إخفاء/أرشفة | ✅ تغيير الحالة | — | |
| تعديل حملة | ✅ | — | |

## 4) الشركاء / الإشعارات / المحتوى

| الفعل | يحفظ بـ Supabase | ملاحظات |
|---|---|---|
| قبول/رفض/تمييز شريك | ✅ (RLS admin) | |
| بثّ إشعار جماعي (broadcast) | ✅ يُدرج إشعارات للجمهور المستهدف | يستلمها كل مستخدم عند المزامنة |
| حذف منشور/تعليق | ✅ (RLS admin) | |
| **بلاغات (reports)** | ⚠️ **محلي فقط — لا يُزامَن** | البلاغ من جهاز مستخدم لا يصل لجهاز الأدمن |

---

## 5) السلاسل المشتقّة (تتفرّع من الأفعال فوق)

- **قبول حملة → ظهورها**: تظهر بالرئيسية/الاستكشاف للمستخدمين (بعد مزامنتهم).
- **انضمام مستخدم → عدّاد المتطوّعين → اكتمال → قيد التنفيذ**: ✅ عبر participants.
- **إنهاء حملة → شهادة + ساعات + إنجازات + رتبة (XP)**: ✅ محسوبة، الشهادات معطّلة بمفتاح.
- **إشعارات غير مقروءة → جرس + شارة لوحة التحكّم**: ✅ تُجلب لكل مستخدم.

---

## 6) ملخّص الفجوات (مرتّبة بالأولوية)

1. **⚠️ لا مزامنة حيّة** — أهم نقطة. الحل: `pullAll` دوري + عند رجوع التركيز
   للتطبيق (visibilitychange) → التغييرات توصل خلال ثوانٍ بدون إعادة فتح يدوي.
2. **⚠️ البلاغات محلية فقط** — تحتاج جدول `reports` + كتابة/قراءة، حتى توصل للأدمن.
3. **⚠️ إشعارات ناقصة** — رفض مستخدم / رفع حظر / تعيين دور بدون إشعار.
4. **⚠️ تاريخ الميلاد** لا يُحفظ عند تعديل الأدمن (مفقود من `userChangesToDb`).
5. ✅ دور الأدمن في قاعدة البيانات (انحلّت — صار حسابك `admin`).

---

## 7) خطة التنفيذ المقترحة (بالترتيب)

1. **المزامنة الحيّة** (يحل 80% من إحساس "مو مترابط"): poll دوري + on-focus pull.
2. **إكمال الإشعارات** الناقصة (رفض/رفع حظر/تعيين دور).
3. **تثبيت تعديلات الأدمن** (تاريخ الميلاد + أي حقل ناقص).
4. **مزامنة البلاغات** (جدول + RLS + write-through).
5. تدقيق نهائي شامل عبر حسابين (أدمن + مستخدم) للتأكد من ترابط كل سلسلة.
